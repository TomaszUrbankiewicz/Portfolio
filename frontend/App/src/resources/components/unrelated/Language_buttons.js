//komponent  strony startowej
import React, { useEffect, useState } from 'react';

const Language_buttons  = (props) => {

    return(
        <section id='language_buttons' className="">
                
        </section>
    );
};
export default Language_buttons ;
